# Docker Production Environment Configuration

# Database Configuration
USE_SQLITE=true
DB_HOST=localhost
DB_PORT=3306
DB_NAME=chemical_compounds
DB_USER=root
DB_PASSWORD=password

# JWT Configuration
JWT_SECRET=SWAYAMLOVESBARCELONA
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration
FRONTEND_URL=http://localhost